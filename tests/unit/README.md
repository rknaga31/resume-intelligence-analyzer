# Unit Tests

Fast, isolated unit tests. No database, no network, no external services.

All external dependencies must be mocked.

## Naming Convention

```python
def test_<component>_<scenario>_<expected_result>():
    pass
```
